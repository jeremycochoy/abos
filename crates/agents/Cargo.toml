[package]
name = "agents"
version = "0.1.0"
edition = "2021"
description = "Agent implementations for agent-based market simulation"

[dependencies]
sim-core = { path = "../sim-core" }
cda-engine = { workspace = true }
rand = { workspace = true }
rand_distr = { workspace = true }
serde = { workspace = true }
